namespace PublishRealLiteApi.Application;

/// <summary>
/// Empty marker interface used solely so other projects (e.g. Program.cs) can reference
/// this assembly via typeof(IApplicationMarker).Assembly for AutoMapper profile scanning
/// without creating a hard coupling to a specific class.
/// </summary>
public interface IApplicationMarker { }